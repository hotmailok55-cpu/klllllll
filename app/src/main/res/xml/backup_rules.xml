<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Never back up the session token: a restored device should sign in
         again rather than inherit a live session. -->
    <exclude domain="sharedpref" path="ljbmk_session.xml" />
    <exclude domain="file" path="datastore/" />
</full-backup-content>
